interface:
  display_name: "知识铸造"
  short_description: "从业务诊断到验收回滚，安全交付可运行的客户知识库操作系统"
  default_prompt: "Use $kb-foundry to diagnose this customer business and design the smallest governed knowledge-base delivery package with a manifest, validation, first-run evidence, and rollback."
policy:
  allow_implicit_invocation: true
